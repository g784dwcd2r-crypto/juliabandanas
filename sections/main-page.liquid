<div class="wrap wrap--narrow">
  <header class="page-head">
    <p class="label page-head__label">{{ shop.name }}</p>
    <h1>{{ page.title }}</h1>
  </header>
  <div class="rte" style="padding-bottom: 80px">{{ page.content }}</div>
</div>

{% schema %}
{ "name": "Page", "settings": [] }
{% endschema %}
